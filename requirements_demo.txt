# Requirements for AI-Powered Honeypot Demo
# This file contains minimal dependencies for running the demo

# No external dependencies required for simple_demo.py
# The simple demo uses only Python standard library

# Optional dependencies for enhanced demo features:
# requests>=2.28.0  # For HTTP requests in enhanced demos
# flask>=2.2.0      # For web dashboard
# paramiko>=2.9.0   # For SSH simulation (advanced demos only)

# For the management dashboard (run_dashboard.py):
# No external dependencies - uses Python's built-in http.server

# Installation instructions:
# 1. For simple demo: No installation needed, just run: python3 simple_demo.py
# 2. For dashboard: No installation needed, just run: python3 run_dashboard.py
# 3. For full system: pip install -r requirements.txt (main requirements file)

# Python version requirement:
# Python 3.7 or higher